Imports System

Module Program
    Sub Main(args As String())
        Dim Num1, Num2, Result As Integer
        Num1 = 12
        Num2 =
        If Nu1 > Num2 Then
            Result = Num1 - Num
        Console.WriteLine("Num1 is bigger than num2 and Result is: " & Result)
        Else
            Result = Num2 - Num1
        Console.WriteLine("Num2 is bigger than num1 and Result is: " & Rsult)
        End If
    End Sub
End Module
